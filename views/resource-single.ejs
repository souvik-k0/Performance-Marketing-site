<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= resource.title %> | BrandBiography
    </title>
    <meta name="description" content="<%= resource.description %>">
    <meta name="robots" content="index, follow">
    <link rel="canonical" href="https://brandbiography.com/resources/<%= resource.slug %>">

    <!-- Open Graph -->
    <meta property="og:type" content="article">
    <meta property="og:title" content="<%= resource.title %>">
    <meta property="og:description" content="<%= resource.description %>">
    <meta property="og:url" content="https://brandbiography.com/resources/<%= resource.slug %>">
    <% if (resource.imageUrl) { %>
        <meta property="og:image" content="<%= resource.imageUrl %>">
        <% } %>

            <!-- Twitter Card -->
            <meta name="twitter:card" content="summary_large_image">
            <meta name="twitter:title" content="<%= resource.title %>">
            <meta name="twitter:description" content="<%= resource.description %>">

            <!-- JSON-LD Article Schema -->
            <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "Article",
      "headline": "<%= resource.title %>",
      "description": "<%= resource.description %>",
      "datePublished": "<%= resource.createdAt %>",
      "author": { "@type": "Organization", "name": "BrandBiography" },
      "publisher": { "@type": "Organization", "name": "BrandBiography" },
      "mainEntityOfPage": { "@type": "WebPage", "@id": "https://brandbiography.com/resources/<%= resource.slug %>" }
    }
    </script>

            <link rel="icon"
                href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>🚀</text></svg>">
            <link rel="preconnect" href="https://fonts.googleapis.com">
            <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
            <link
                href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&family=Outfit:wght@400;500;600;700;800&display=swap"
                rel="stylesheet">
            <script src="https://unpkg.com/@phosphor-icons/web"></script>
            <link rel="stylesheet" href="/styles.css">
            <style>
                .article-hero {
                    padding: 8rem 0 3rem;
                    background: linear-gradient(135deg, #fff5f5 0%, #fef2f2 40%, #fff 100%);
                }

                .article-hero .container {
                    max-width: 820px;
                    margin: 0 auto;
                    padding: 0 1.5rem;
                }

                .article-breadcrumb {
                    display: flex;
                    align-items: center;
                    gap: .5rem;
                    font-size: .85rem;
                    color: var(--text-muted);
                    margin-bottom: 1.5rem;
                }

                .article-breadcrumb a {
                    color: var(--primary);
                    text-decoration: none;
                }

                .article-breadcrumb a:hover {
                    text-decoration: underline;
                }

                .article-type-badge {
                    display: inline-flex;
                    align-items: center;
                    gap: .35rem;
                    padding: .35rem .85rem;
                    border-radius: 999px;
                    font-size: .78rem;
                    font-weight: 600;
                    text-transform: uppercase;
                    letter-spacing: .05em;
                    margin-bottom: 1rem;
                }

                .article-type-badge.case-study {
                    background: rgba(239, 68, 68, .08);
                    color: #ef4444;
                }

                .article-type-badge.blog {
                    background: rgba(59, 130, 246, .08);
                    color: #3b82f6;
                }

                .article-hero h1 {
                    font-family: 'Outfit', sans-serif;
                    font-size: clamp(1.75rem, 4vw, 2.5rem);
                    font-weight: 800;
                    line-height: 1.2;
                    color: var(--text-main);
                    margin-bottom: 1rem;
                }

                .article-hero .article-desc {
                    font-size: 1.1rem;
                    color: var(--text-muted);
                    line-height: 1.6;
                    margin-bottom: 1.5rem;
                }

                .article-meta {
                    display: flex;
                    align-items: center;
                    gap: 1.5rem;
                    font-size: .85rem;
                    color: var(--text-muted);
                }

                .article-meta span {
                    display: flex;
                    align-items: center;
                    gap: .35rem;
                }

                .article-cover {
                    max-width: 820px;
                    margin: 0 auto;
                    padding: 0 1.5rem;
                }

                .article-cover img {
                    width: 100%;
                    border-radius: 16px;
                    box-shadow: 0 12px 40px rgba(0, 0, 0, .08);
                    margin-top: -1rem;
                }

                .cover-placeholder {
                    width: 100%;
                    height: 280px;
                    border-radius: 16px;
                    background: linear-gradient(135deg, #fef2f2 0%, #fee2e2 30%, #fecaca 70%, #fca5a5 100%);
                    display: flex;
                    align-items: center;
                    justify-content: center;
                    margin-top: -1rem;
                }

                .cover-placeholder i {
                    font-size: 5rem;
                    color: rgba(239, 68, 68, .25);
                }

                .article-body {
                    max-width: 720px;
                    margin: 3rem auto;
                    padding: 0 1.5rem;
                    font-family: 'Inter', sans-serif;
                    font-size: 1.05rem;
                    line-height: 1.85;
                    color: var(--text-main);
                }

                .article-body h2 {
                    font-family: 'Outfit', sans-serif;
                    font-size: 1.5rem;
                    font-weight: 700;
                    margin: 2.5rem 0 1rem;
                    color: var(--text-main);
                }

                .article-body h3 {
                    font-family: 'Outfit', sans-serif;
                    font-size: 1.2rem;
                    font-weight: 600;
                    margin: 2rem 0 .75rem;
                    color: var(--text-main);
                }

                .article-body p {
                    margin-bottom: 1.25rem;
                }

                .article-body ul,
                .article-body ol {
                    margin: 1rem 0 1.5rem 1.5rem;
                }

                .article-body li {
                    margin-bottom: .5rem;
                }

                .article-body strong {
                    font-weight: 700;
                }

                .article-body table {
                    width: 100%;
                    border-collapse: collapse;
                    margin: 1.5rem 0;
                    font-size: .95rem;
                }

                .article-body th,
                .article-body td {
                    padding: .75rem 1rem;
                    text-align: left;
                    border-bottom: 1px solid #e2e8f0;
                }

                .article-body th {
                    font-weight: 600;
                    color: var(--text-main);
                    background: #f8fafc;
                }

                .article-body blockquote {
                    border-left: 4px solid var(--primary);
                    padding: 1rem 1.5rem;
                    margin: 1.5rem 0;
                    background: rgba(239, 68, 68, .03);
                    border-radius: 0 12px 12px 0;
                    font-style: italic;
                    color: var(--text-muted);
                }

                .article-cta {
                    max-width: 720px;
                    margin: 0 auto 4rem;
                    padding: 2.5rem;
                    background: linear-gradient(135deg, var(--primary), #c62828);
                    border-radius: 20px;
                    text-align: center;
                    color: #fff;
                }

                .article-cta h3 {
                    font-family: 'Outfit', sans-serif;
                    font-size: 1.5rem;
                    font-weight: 700;
                    margin-bottom: .75rem;
                }

                .article-cta p {
                    opacity: .9;
                    margin-bottom: 1.5rem;
                }

                .article-cta .btn {
                    display: inline-flex;
                    align-items: center;
                    gap: .5rem;
                    padding: .85rem 2rem;
                    background: #fff;
                    color: var(--primary);
                    border-radius: 12px;
                    font-weight: 700;
                    font-size: .95rem;
                    text-decoration: none;
                    transition: transform .2s, box-shadow .2s;
                }

                .article-cta .btn:hover {
                    transform: translateY(-2px);
                    box-shadow: 0 8px 24px rgba(0, 0, 0, .2);
                }

                .back-link {
                    display: inline-flex;
                    align-items: center;
                    gap: .4rem;
                    color: var(--primary);
                    font-weight: 600;
                    font-size: .9rem;
                    text-decoration: none;
                    margin-bottom: 2rem;
                }

                .back-link:hover {
                    text-decoration: underline;
                }

                @media (max-width: 600px) {
                    .article-hero {
                        padding: 6rem 0 2rem;
                    }

                    .article-body {
                        font-size: .97rem;
                    }

                    .article-cta {
                        margin: 0 1rem 3rem;
                        padding: 2rem 1.5rem;
                    }
                }
            </style>
</head>

<body>
    <div class="bg-mesh">
        <div class="mesh-blob blob-1"></div>
        <div class="mesh-blob blob-2"></div>
    </div>

    <!-- Navbar (matches main site) -->
    <nav class="navbar" id="navbar">
        <div class="container nav-content">
            <a href="/" class="logo">
                <i class="ph-fill ph-book-open-text"></i>
                <span>BrandBiography</span>
            </a>
            <div class="nav-links">
                <a href="/#solutions">Solutions</a>
                <a href="/pricing.html">Pricing</a>
                <a href="/resources">Resources</a>
                <a href="/about.html">About Us</a>
            </div>
            <a href="/#demo" class="btn btn-primary btn-glow nav-cta">Book a Demo</a>
            <button class="mobile-menu-btn" id="menu-toggle" aria-label="Toggle menu"><i
                    class="ph ph-list"></i></button>
        </div>
        <div class="mobile-drawer" id="mobile-drawer">
            <a href="/#solutions" class="drawer-link">Solutions</a>
            <a href="/pricing.html" class="drawer-link">Pricing</a>
            <a href="/resources" class="drawer-link">Resources</a>
            <a href="/about.html" class="drawer-link">About Us</a>
            <a href="/#demo" class="btn btn-primary btn-full drawer-cta">Book a Demo</a>
        </div>
    </nav>

    <!-- Article Hero -->
    <section class="article-hero">
        <div class="container">
            <nav class="article-breadcrumb" aria-label="Breadcrumb">
                <a href="/">Home</a>
                <i class="ph ph-caret-right"></i>
                <a href="/resources">Resources</a>
                <i class="ph ph-caret-right"></i>
                <span>
                    <%= resource.type==='case-study' ? 'Case Study' : 'Blog' %>
                </span>
            </nav>

            <span class="article-type-badge <%= resource.type %>">
                <i class="ph-fill <%= resource.type === 'case-study' ? 'ph-trend-up' : 'ph-pencil-line' %>"></i>
                <%= resource.type==='case-study' ? 'Case Study' : 'Blog Post' %>
            </span>

            <h1>
                <%= resource.title %>
            </h1>
            <p class="article-desc">
                <%= resource.description %>
            </p>

            <div class="article-meta">
                <span><i class="ph ph-calendar-blank"></i>
                    <%= new Date(resource.createdAt).toLocaleDateString('en-US', { month: 'long' , day: 'numeric' ,
                        year: 'numeric' }) %>
                </span>
                <span><i class="ph ph-clock"></i>
                    <%= Math.max(1, Math.ceil((resource.content || '' ).split(' ').length / 200)) %> min read</span>
            </div>
        </div>
    </section>

    <!-- Cover Image -->
    <div class="article-cover">
        <% if (resource.imageUrl) { %>
        <img src="<%= resource.imageUrl %>" alt="<%= resource.title %>" width="820" height="460" loading="eager">
        <% } else { %>
        <div class="cover-placeholder">
            <i class="ph-duotone <%= resource.type === ' case-study' ? 'ph-chart-line-up' : 'ph-article' %>"></i>
            </div>
            <% } %>
        </div>

        <!-- Article Body -->
        <article class="article-body">
            <a href="/resources" class="back-link"><i class="ph ph-arrow-left"></i> All Resources</a>
            <%- contentHtml %>
        </article>

        <!-- CTA -->
        <div class="article-cta">
            <h3>Ready to see similar results?</h3>
            <p>Book a free personalized demo and discover how BrandBiography can transform your ad performance.</p>
            <a href="/#demo" class="btn"><i class="ph-fill ph-rocket-launch"></i> Book My Free Demo</a>
        </div>

        <script>
            // Navbar scroll effect
            const navbar = document.getElementById('navbar');
            window.addEventListener('scroll', () => navbar.classList.toggle('scrolled', window.scrollY > 50));
            // Mobile menu
            const menuToggle = document.getElementById('menu-toggle');
            const mobileDrawer = document.getElementById('mobile-drawer');
            if (menuToggle && mobileDrawer) {
                menuToggle.addEventListener('click', () => {
                    mobileDrawer.classList.toggle('open');
                    menuToggle.querySelector('i').className = mobileDrawer.classList.contains('open') ? 'ph ph-x' : 'ph ph-list';
                });
            }
        </script>
</body>

</html>